# Screenshots & Generated Graphs

Letakkan di folder ini:

1. Screenshot hasil `explain("executionStats")` dari mongosh/Compass
   (sebelum dan sesudah indexing).
2. Tiga grafik hasil `report/generate_graphs.py`:
   - `query_time_before_after.png`
   - `docs_examined_before_after.png`
   - `execution_time_per_query.png`

Gambar-gambar ini digunakan sebagai bukti visual pada
`report/report_template.md`.
